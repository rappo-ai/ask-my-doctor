version: "2.0"

nlu:
  - intent: EXTERNAL_doctor_signup
    metadata:
      keyword_intent: true
      match_condition: "exact"
    examples: |
      - /signup
  - intent: EXTERNAL_on_google_auth
    metadata:
      keyword_intent: true
      match_condition: "exact"
    examples: |
      - /google
