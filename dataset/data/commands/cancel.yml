version: "2.0"

actions:
  - action_cancel

intents:
  - cancel

nlu:
  - intent: cancel
    metadata:
      keyword_intent: true
      match_condition: "exact"
    examples: |
      - /cancel

rules:
  - rule: Command /cancel
    steps:
      - intent: cancel
      - action: action_cancel
