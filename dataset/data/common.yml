version: "2.0"

intents:
  - affirm
  - deny

nlu:
  - intent: affirm
    metadata:
      keyword_intent: true
      match_condition: "exact"
    examples: |
      - Yes
  - intent: deny
    metadata:
      keyword_intent: true
      match_condition: "exact"
    examples: |
      - No
